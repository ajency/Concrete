{% section 'blog-header' %}
{% section 'blog-template' %}
{% capture blog_url %} {{ blog.url }}/tagged/{{ tag | handle }}{% endcapture %}
{% unless blog_url %}
  {% section 'featured-blog-listing' %}
{% endunless %}
<!-- 
<!-- 
{% paginate blog.articles by 5 %}

  {%- assign blog_title = blog.title -%}

  {% if current_tags %}
    {% capture blog_title %}{{ blog.title | link_to: blog.url }} &mdash; {{ current_tags.first }}{% endcapture %}
  {% endif %}
  <div class="blog-category blog-listing">
    <div class="latest-articles-cards">
      <div class="container">
          <h1 class="title">{{ blog_title }}</h1>
            {% if blog.all_tags.size > 0 %}
              <!-- <ul>
                {% for tag in blog.all_tags %}

                  {% if current_tags contains tag %}
                    <li>{{ tag }}</li>
                  {% else %}
                    <li>{{ tag | link_to_tag: tag }}</li>
                  {% endif %}

                {% endfor %}
              </ul> -->
            <!-- {% endif %}

            {% comment %}
              Article Previews
              ====================
            {% endcomment %}
       <div class="row">
          {% for article in blog.articles %}
          <div class="article-card col-md-4 col-sm-12">
            
              {% if article.image %}
                <a href="{{ article.url }}">
                  {% render 'responsive-image' with
                    image: article.image,
                    max_width: 1024,
                    max_height: 1024
                    class: 'article-featured-img'
                  %}
                </a>
              {% endif %}
              <div class="article-body">
                <div class="content-body">
                  <h3 class="title">
                    <a href="{{ article.url }}">{{ article.title }}</a>
                  </h3>
                  <p class="date">
                    {{ 'blogs.article.author_on_date_html' | t: author: article.author, date: date }}
                  </p>
                </div>
                <div class="rte content">
                  {% if article.excerpt.size > 0 %}
                    {{ article.excerpt }}
                  {% else %}
                    <p>
                      {{ article.content | strip_html | truncatewords: 100 }}
                    </p>
                  {% endif %}
                </div>

                {% if blog.comments_enabled? or article.tags.size > 0 %}
                  <ul class="blog-tags-list">
                    {% if blog.comments_enabled? %}
                      <li>
                        <a href="{{ article.url }}#comments">
                          {{ 'blogs.comments.with_count' | t: count: article.comments_count }}
                        </a>
                      </li>
                    {% endif %}

                    {% if article.tags.size > 0 %}
                      {% for tag in article.tags %}
                        <li class="blog-tags">
                          <a href="{{ blog.url }}/tagged/{{ tag | handle }}">{{ tag }}</a>
                          {% unless forloop.last %}, {% endunless %}
                        </li>
                      {% endfor %}
                    {% endif %}
                  </ul>
                {% endif %}
            </div>
          </div>
            {% endfor %}
            {% if paginate.pages > 1 %}
              {% render 'pagination' %}
            {% endif %}
        </div>
      </div>
    </div>
  </div> -->
{% endpaginate %}
